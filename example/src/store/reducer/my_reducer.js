const INITIAL_STATE = {
    userName: 'Alex Josh'
}
//static data for now   

export default (state = INITIAL_STATE, action)=>{
    switch(action.type){
        default:
            return state;  
    }
}